{% extends 'base-back-office.html.twig' %}


{% block mainContent %}
	<h1>Product</h1>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Price</th>
                <th>Description</th>
                <th>Quantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
       
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.name }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.quantity }}</td>
                <td>
                    
                    <a href="{{ path('product_edit', {'id': product.id}) }}", button class="btn btn-warning">edit</a>
                    <a href="{{ path('product_delete', {'id': product.id}) }}", button class="btn btn-danger">delete</a>
                    <a href="{{ path('product_index') }}",  button class="btn btn-outline-default" >Back to list</a>
                </td>
            </tr>
        
            
        
	{# <table class="table">
    
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ product.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ product.name }}</td>
			</tr>
			<tr>
				<th>Price</th>
				<td>{{ product.price }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ product.description }}</td>
			</tr>
			<tr>
				<th>Quantity</th>
				<td>{{ product.quantity }}</td>
			</tr>
			 #}

		
	</table>
	<div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel" style="
			    width: 300px;
			    margin-left: auto;
			    margin-right: auto;">
		<div class="carousel-indicators">
		</div>
		<div class="carousel-inner">
		
					<img src="{{ asset('/uploads/products/' ~ product.image) }}" class="d-block w-100" alt="...">
				</div>
				{# <div>
																			<img src="" alt="Image" width="150">
																			<a href="{{ path('product_delete_image', {id: image.id}) }}" data-delete data-token="{{ csrf_token('delete' ~ image.id) }}">Delete</a>
																		</div> #}

		

		</div>
		<button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
			<span class="carousel-control-prev-icon" aria-hidden="true"></span>
			<span class="visually-hidden">Previous</span>
		</button>
		<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
			<span class="carousel-control-next-icon" aria-hidden="true"></span>
			<span class="visually-hidden">Next</span>
		</button>
	</div>



    
    
{# <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    <a href="{{ path('product_index') }}", button class="btn btn-outline-default" >Back to products</a> #}

	
{% endblock %}

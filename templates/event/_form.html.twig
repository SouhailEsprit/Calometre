
{{ form_start(form,{'attr': {'novalidate': 'novalidate' , 'class':'was-validated'}} ) }}
    {{ form_row(form.nom) }}

    {{ form_row(form.dateDebut, { 'id': 'datepicker' }) }}

    {{ form_row(form.dateFin, { 'id': 'datepicker1' }) }}

    {{ form_row(form.description)}}

    {{ form_row(form.nombreParticipants, { 'type': 'number' }) }}

    {{ form_row(form.lieu) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

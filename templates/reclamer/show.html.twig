
{% extends 'base-back-office.html.twig' %}
   {% block mainContent %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamer.id }}</td>
            </tr>
            <tr>
                <th>Idreclamation</th>
                <td>{{ reclamer.idreclamation }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamer.type }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ reclamer.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamer_index') }}">Retour</a>

    <a href="{{ path('reclamer_edit', {'id': reclamer.id}) }}">modifier</a>

    {{ include('reclamer/_delete_form.html.twig') }}

{% endblock %}
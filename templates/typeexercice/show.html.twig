{% extends 'base-back-office.html.twig' %}

{% block mainContent %}
    <h1>Typeexercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeexercice.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ typeexercice.nom }}</td>
            </tr>
        </tbody>
    </table>
<div>
    <a href="{{ path('typeexercice_index') }}"button class="btn btn-light">back to list</a>

    <a href="{{ path('typeexercice_edit', {'id': typeexercice.id}) }}"button class="btn btn-light">edit</a>

    {{ include('typeexercice/_delete_form.html.twig') }}
    </div>
{% endblock %}
